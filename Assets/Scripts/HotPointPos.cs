using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HotPointPos : MonoBehaviour
{
    /// <summary>
    /// 指定热点位置的trasnform
    /// </summary>
    //public Transform part;
    // Start is called before the first frame update
    void Start()
    {
        //transform.position = Camera.main.ScreenToWorldPoint(part.position);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
